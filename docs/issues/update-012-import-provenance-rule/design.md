# update-012 反映設計書: テンプレートへの取り込み時に「経緯と目的」の精査を必須化する（update 案件の反映の Sonnet 委任化を含む）

- **案件**: update-012（本リポジトリ発の改訂。反映元リポジトリなし）
- **作成日**: 2026-09-06
- **調査記録**: 同フォルダの `README.md`（契機・目的・採らなかった案・保持すべき制約・現行文面の調査）。本書は調査結果を前提とする
- **反映先の基準コミット**: 本リポジトリ `6f5697b`（ルート `CLAUDE.md` 81行、ルート `AGENTS.md` 26行、`template/CLAUDE.md` 421行）

## 0. 本書の使い方

本書は `/clear` 後でも本書だけで反映作業が完結するように書かれている。反映者は「2. 変更（D1〜D4）」を順に適用し、「3. 反映後の検証」で確認し、「4. 完了処理」を実施する。各変更は「位置」「変更前（全文）」「変更後（全文）」で示す。位置の指定は見出しと前後の文で行い、行番号は参考（基準コミット時点の値）。変更前のテキストは現行ファイルに一意に存在する（反映前に一致を確認すること）。

## 1. 方針

- **実施者**: D3〜D6（`template/CLAUDE.md`）はルート `CLAUDE.md` ステップ5 の規定どおり Sonnet サブエージェントに委任する（変更量の多寡で委任の要否を変えない。委任の目的は design.md だけで作業できるかの独立検証にある）。D1・D2（ルート `CLAUDE.md`・ルート `AGENTS.md`）は Claude Code 本体（Sonnet にはルート `CLAUDE.md` の更新をさせない規定のため。`AGENTS.md` も同じ扱いにする）。適用順は Sonnet の D3〜D6 → 本体の D1・D2 とする
- **テスト**: コード変更がないため不要
- **変更対象ファイル**: ルート `CLAUDE.md`（D1）、ルート `AGENTS.md`（D2）、`template/CLAUDE.md`（D3〜D6）、（完了処理で）本案件 `README.md`
- **変更しないファイル**: `template/AGENTS.md`、`template/docs/` 配下全部、`template/.gitignore`、他案件の文書
- **変更方式**: 行内置換のみ（D1 は連続する3行、D2 は連続する4行、D3〜D6 は各1行）。各項に変更前・変更後の全文を記載
- **汎用化の置換**: 本案件は反映元の文面を転記しないため該当なし
- **保持すべき制約**（README「背景・変更理由」）: ルート `AGENTS.md` の2段階判定・「判定不能は高で報告し、残る間は収束扱いにしない」・`reviews/` と未指定文書の除外の文言は D2 で変えない。D2 は括弧内の再掲を削るだけで、判定の情報源（`CLAUDE.md` のステップ2・3）は変わらない

## 2. 変更（D1〜D6）

適用順は D3 → D4 → D5 → D6（Sonnet）→ D1 → D2（本体）とする。各項は互いに独立しており、順序で結果は変わらない。

### D1: ルート `CLAUDE.md`「テンプレート改訂フロー（update-XXX 案件）」ステップ1〜3（行内置換。連続する3行）

**位置**: 「## テンプレート改訂フロー（update-XXX 案件）」の番号付き箇条書きの 1〜3（参考: 59〜61行）

**変更前**（全文）:

```
1. **案件作成** → `docs/issues/update-{number}-{slug}/` フォルダを作成する（例: `update-001-from-lift2d`。slug には反映元がわかる名前を付ける）
2. **調査・計画** → 反映元リポジトリ（ユーザーがパスを指定する）のドキュメントとテンプレートを比較し、差分を洗い出す。汎用的な内容のみを反映対象として選別し（プロジェクト固有の記述は除外）、反映元パス・調査結果・反映対象の選別と理由を案件フォルダの `README.md` に記録する。**この時点ではテンプレートを編集しない**
3. **設計・保存** → どのテンプレートファイルに・どのように反映するかを、案件フォルダの `design.md` に書いてファイル保存する（自己完結・曖昧表現禁止。/clear 後でも design.md だけ読めば反映作業ができるレベルまで具体化する）。**保存が完了するまでテンプレートの編集に進んではならない**
```

**変更後**（全文）:

```
1. **案件作成** → `docs/issues/update-{number}-{slug}/` フォルダを作成する。slug は、反映元リポジトリがある場合は反映元がわかる名前（例: `update-001-from-lift2d`）、反映元がない本リポジトリ発の改訂では変更の目的がわかる名前（例: `update-007-repo-herdr-alignment`）を付ける
2. **調査・計画** → 反映元リポジトリ（ユーザーがパスを指定する）のドキュメントとテンプレートを比較し、差分を洗い出す。差分の候補ごとに、反映元の最終ファイルだけでなく、その候補を導入した反映元の案件フォルダの文書（`README.md` の背景・変更理由・採らなかった案・人レビューの記録、`design.md` の改訂履歴、`reviews/` の結果〔全文ゲートの回と行き詰まり検出の回を含む〕）を読み、**経緯と目的を精査する**。汎用的な内容のみを反映対象として選別し（プロジェクト固有の記述は除外）、反映元パス・調査結果・反映対象の選別と理由を案件フォルダの `README.md` に記録する。取り込む候補ごとに次の5項目を `README.md` に記録する: (a) 契機（反映元で何が起きて必要になったか）(b) 目的（何を担保するか）(c) 形式の理由（なぜその文面・構造か。採らなかった案と不採用の理由。レビュー指摘で確定した文言）(d) 保持すべき制約（汎用化で変えてはいけない部分）(e) 典拠（反映元の案件文書・レビュー結果のパス）。経緯が反映元の文書から確認できない候補は、推測で目的を補わず、ユーザーに確認するか取り込まない（確認できないことと、その扱いを `README.md` に記録する）。反映元リポジトリがない案件（本リポジトリ発の改訂）では、比較の代わりに現状と変更理由を調査し、(a)〜(d) を `README.md` に記録する（(e) は本リポジトリ内の典拠）。**この時点ではテンプレートを編集しない**
3. **設計・保存** → どのテンプレートファイルに・どのように反映するかを、案件フォルダの `design.md` に書いてファイル保存する（自己完結・曖昧表現禁止。/clear 後でも design.md だけ読めば反映作業ができるレベルまで具体化する）。反映元の文面を汎用化のために変える置換は、1件ごとに変更前・変更後と、ステップ2 の (d) 保持すべき制約を損なわない理由を `design.md` に書く。**保存が完了するまでテンプレートの編集に進んではならない**
```

（ステップ1 の変更点: slug の規定を、反映元がある場合とない場合で分ける〔codex-01 の指摘〕。ステップ2 の追加点: 精査で読む対象の明示、記録する5項目 (a)〜(e)、確認できない候補の扱い、反映元がない案件の扱い。ステップ3 の追加点: 汎用化の置換ごとに (d) を損なわない理由を書く。既存の文はすべて残す。）

### D2: ルート `AGENTS.md` 規定適合確認の項目の「対象文書単位」の文（行内置換。連続する4行 → 2行）

**位置**: 「## レビューを依頼された場合」の4項目目、「2. 対象文書単位: 依頼で指定された各対象文書に、規定された内容があり、書いてはならない内容が」の行の直後の4行（参考: 23〜26行。ファイル末尾まで）

**変更前**（全文）:

```
     含まれていないか。`README.md` はステップ2 が定める内容（反映元パス・調査結果・反映対象の選別と
     理由）、`design.md` はステップ3 が定める内容（どのテンプレートファイルに・どのように反映するか。
     自己完結・曖昧表現禁止。design.md だけ読めば反映作業ができること）に従う（判定対象は依頼で
     指定された文書のみ。`reviews/` 配下の結果文書と、依頼で指定されていない文書は対象外）
```

**変更後**（全文）:

```
     含まれていないか。`README.md` はステップ2 が定める内容、`design.md` はステップ3 が定める内容に従う
     （判定対象は依頼で指定された文書のみ。`reviews/` 配下の結果文書と、依頼で指定されていない文書は対象外）
```

（括弧内に再掲していたステップ2・3 の内容を削り、`CLAUDE.md` の当該ステップを情報源として参照する形にする。「判定対象は依頼で指定された文書のみ…」の文は変えない。）

### D3: `template/CLAUDE.md`「ドキュメント更新フロー（update-XXX 案件）」ステップ2（行内置換）

**位置**: 「### ドキュメント更新フロー（update-XXX 案件）」の番号付き箇条書きの 2（参考: 158行）

**変更前**（全文）:

```
2. **調査** → 現状と変更理由を調査し、案件フォルダの `README.md` に記録する。テンプレート取り込みの場合は反映元パス・コミットID・差分の全量と、取り込む/取り込まない の選別と理由を書く。**この時点で反映先を編集してはならない**
```

**変更後**（全文）:

```
2. **調査** → 現状と変更理由を調査し、案件フォルダの `README.md` に記録する。変更理由には、契機（何が起きて必要になったか）・目的（何を担保するか）・採らなかった案と不採用の理由を含め、人レビューで方針が変わった場合はその記録も残す（コピー元テンプレートへの還元時に、テンプレート側が経緯と目的を精査する典拠になる）。テンプレート取り込みの場合は反映元パス・コミットID・差分の全量と、取り込む/取り込まない の選別と理由を書く。**この時点で反映先を編集してはならない**
```

### D4: `template/CLAUDE.md`「ドキュメント更新フロー」配下「#### 運用メモ」の1項目目（行内置換）

**位置**: 「#### 運用メモ」の箇条書きの1項目目「- 汎用性のある改善は、…」（参考: 170行）

**変更前**（全文）:

```
- 汎用性のある改善は、完了後にコピー元の開発ドキュメントテンプレートリポジトリへの還元（テンプレート側の update-XXX 案件）を検討する
```

**変更後**（全文）:

```
- 汎用性のある改善は、完了後にコピー元の開発ドキュメントテンプレートリポジトリへの還元（テンプレート側の update-XXX 案件）を検討する。還元時はテンプレート側が案件 `README.md` の変更理由（契機・目的・採らなかった案）と `reviews/` の結果を典拠に経緯と目的を精査するため、これらを完了後に削除・要約しない
```

### D5: `template/CLAUDE.md`「ドキュメント更新フロー（update-XXX 案件）」ステップ5（行内置換）

**位置**: 「### ドキュメント更新フロー（update-XXX 案件）」の番号付き箇条書きの 5（参考: 164行）

**変更前**（全文）:

```
5. **反映** → design.md に厳密に従って編集する。実装は Claude Code 本体が行ってよい（転記・削除中心で Sonnet 委任のオーバーヘッドに見合わないため。分量が大きい機械的変更では委任も可。どちらにするかは design.md に明記する）。設計にない変更が必要になったら中断してステップ2に戻る。反映後 `git diff` で「意図した変更のみか・保持対象が変わっていないか」を検証する
```

**変更後**（全文）:

```
5. **反映** → design.md に厳密に従って編集する。反映は後述の「実装の実行方法（Sonnetサブエージェント）」に従い、Sonnet サブエージェントに委任する（変更量の多寡で委任の要否を変えない。委任の目的は design.md の自己完結性の独立検証にある）。読み替え: 必読ドキュメントは CLAUDE.md → 案件 `README.md` → `design.md` → 変更対象ファイル。テスト実行は不要。禁止事項のうち「CLAUDE.md … の更新も行わせない」は design.md に変更対象として明記したファイルには適用しない（design.md に記載のない変更を行わせない）。想定外事象の扱いの復帰先「調査・計画 → requirements.md / design.md（または investigation.md）の修正」は、update 案件では「ステップ2 の調査 → `README.md` / `design.md` の更新 → ステップ4 のレビュー」と読む。厳密準拠・想定外事象での即中断報告・git 操作の禁止・報告形式はそのまま従う。設計にない変更が必要になったら中断してステップ2に戻る。反映後 `git diff` で「意図した変更のみか・保持対象が変わっていないか」を Claude Code 本体が検証する
```

（「実装は Claude Code 本体が行ってよい（…オーバーヘッドに見合わないため…）」の但し書きを削除し、feat / bug のステップ6 と同じく無条件の Sonnet 委任にする。update 案件では変更対象が `CLAUDE.md` 自体になり得るため、「実装の実行方法」の禁止事項5 の読み替えを明記する。ルート `CLAUDE.md`「テンプレート改訂フロー」ステップ5 の読み替えと同じ構造。人レビューの指摘〔2026-09-07〕で追加。）

### D6: `template/CLAUDE.md`「実装の実行方法（Sonnetサブエージェント）」の冒頭段落（行内置換）

**位置**: 「### 実装の実行方法（Sonnetサブエージェント）」の見出し直後の段落（参考: 349行）

**変更前**（全文）:

```
機能追加・不具合修正フローのステップ6（実装）は、Claude Code 自身が直接コードを書くのではなく、Agent ツールで **model: sonnet** を指定したサブエージェントに委任する。 委任の目的は、会話の文脈を持たない実装者に設計書だけで実装させることで、設計書が自己完結しているかを独立に検証することにある。実装者が詰まったら設計書の欠陥として扱い、項目3 に従って設計に戻る。
```

**変更後**（全文）:

```
機能追加・不具合修正フローのステップ6（実装）とドキュメント更新フローのステップ5（反映。読み替えは同ステップに記す）は、Claude Code 自身が直接コードを書くのではなく、Agent ツールで **model: sonnet** を指定したサブエージェントに委任する。 委任の目的は、会話の文脈を持たない実装者に設計書だけで実装させることで、設計書が自己完結しているかを独立に検証することにある。実装者が詰まったら設計書の欠陥として扱い、項目3 に従って設計に戻る。
```

（適用範囲にドキュメント更新フローのステップ5 を加える。他の文は変えない。）


## 3. 反映後の検証（Claude Code 本体が実施。Sonnet の報告を受けた後、D1・D2 を適用してから行う）

1. `git diff --stat` の変更ファイルが `CLAUDE.md`・`AGENTS.md`・`template/CLAUDE.md` の3件（＋本案件フォルダ）のみであること
2. `git diff -U0` のハンクが、ルート `CLAUDE.md` はステップ1〜3 の3行、ルート `AGENTS.md` は末尾4行、`template/CLAUDE.md` はドキュメント更新フローのステップ2・ステップ5・運用メモの1項目目・「実装の実行方法」冒頭段落の4行だけであること
3. ルート `CLAUDE.md` ステップ1 に「反映元がない本リポジトリ発の改訂では変更の目的がわかる名前」、ステップ2 に「(a) 契機」「(b) 目的」「(c) 形式の理由」「(d) 保持すべき制約」「(e) 典拠」「確認できない候補」「反映元リポジトリがない案件」の記述があり、ステップ3 に「(d) 保持すべき制約を損なわない理由」の記述があること
4. ルート `AGENTS.md` に「反映元パス・調査結果・反映対象の選別と理由」「design.md だけ読めば反映作業ができること」の文字列が残っていないこと。「判定不能」「収束扱いにしない」「`reviews/` 配下の結果文書」の文言が残っていること
5. `template/CLAUDE.md` の行数が 421 行のままであること（行内置換のみ）。`grep -c '契機' template/CLAUDE.md` が 2 であること（ステップ2 と運用メモ）。`grep -c 'オーバーヘッドに見合わない' template/CLAUDE.md` が 0 であること。「実装の実行方法」冒頭段落に「ドキュメント更新フローのステップ5」の記述があること
6. 本案件の `README.md` が、D1 で定める (a)〜(e) を満たしていること（「背景・変更理由」節に契機・目的・形式の理由と採らなかった案・保持すべき制約、「反映元」節に典拠）

## 4. 完了処理（検証後に Claude Code 本体が実施）

1. 本案件 `README.md` のステータスを `Open（…）` → `Closed（2026-09-06 起票、YYYY-MM-DD 完了。Codex レビュー N 回で高・中ゼロ収束、人レビュー承認済み、design.md どおり反映・検証済み）` に更新し、実施記録に反映・検証の結果を追記する
2. ルート `CLAUDE.md` のディレクトリ構成: ファイルの追加・削除はないため変更しない（D1 で本文のみ変更）
3. Herdr のレビューストリーム `rev-devtmpl-update-012` に `/quit` を送り、`herdr pane close {pane_id}` でペインを撤去する。ルート `AGENTS.md` は起動時にのみ読まれるため、D2 は次に起動するストリームから有効になる
4. git 操作: ルート `CLAUDE.md`「Claude Code 運用ルール」に従い Opus サブエージェントに委任してコミットする（push はユーザー指示があるときのみ）
5. update-011 design.md「6. 動作確認」の結果（本案件の codex-01 に「[AGENTS.md適用]」マーカーと規定適合の判定が含まれたか）を本案件 `README.md` の実施記録に書く

## 改訂履歴

- 2026-09-06: 初版（Codex レビュー前）
- 2026-09-07: codex-06 指摘対応（高: D5 が「実装の実行方法」を update 案件に適用する一方、同節の想定外事象の復帰先が requirements.md / investigation.md のままで update 案件では実行不能 → D5 の読み替えに復帰先「ステップ2 の調査 → README.md / design.md の更新 → ステップ4 のレビュー」を追加）
- 2026-09-07: 人レビュー指摘対応（2件目: `template/CLAUDE.md` ドキュメント更新フロー ステップ5 の「実装は Claude Code 本体が行ってよい（オーバーヘッドに見合わないため）」の但し書きが、lift2d update-007〔feat / bug のステップ6 に限定〕でも本リポジトリ update-010〔案件文書の修正止まり〕でも削除されずに残っていたため、本案件に D5〔無条件の Sonnet 委任と読み替え〕・D6〔「実装の実行方法」の適用範囲〕を追加。方針・検証2・5 を同期）
- 2026-09-07: 人レビュー指摘対応（1件目: 「1. 方針」で D3・D4 を本体が行う例外を置いていたが、根拠は「オーバーヘッドに見合わない」という変更量の多寡であり、委任の目的〔design.md の自己完結性の独立検証〕と無関係。例外を撤回し規定どおり Sonnet に委任する。適用順を同期）
- 2026-09-06: codex-01 指摘対応（高: D1 が「反映元がない案件」を扱う一方、ステップ1 は slug に反映元がわかる名前を必須としており矛盾 → D1 の範囲をステップ1〜3 に広げ、slug の規定を反映元の有無で分けた。方針・検証2・3 を同期）

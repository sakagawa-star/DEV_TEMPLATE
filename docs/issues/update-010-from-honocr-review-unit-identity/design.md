# update-010 反映設計書

- 案件: `docs/issues/update-010-from-honocr-review-unit-identity/`
- 調査記録: `README.md`（本ファイルと同じフォルダ）
- 作成日: 2026-09-05

## 1. 反映のスコープ

**変更するファイルは `template/CLAUDE.md` の1ファイルのみ。** 変更は3箇所の置換（項目1・2）と
2箇所の行末追記（項目3）で、合計4箇所・5行に影響する。ファイルの新規作成・削除はない。

以下は変更しない:

- `template/AGENTS.md`、`template/.gitignore`、`template/docs/` 配下のすべて
- 本リポジトリのルート `CLAUDE.md`（ディレクトリ構成に変更がないため。§6 で確認する）
- honOCR 側のファイル（本案件は DEV_TEMPLATE 側のみで実施する）

## 2. 反映の実施者と方式

**Sonnet サブエージェントに委任する。** 本案件に適用されるのは本リポジトリのルート `CLAUDE.md`
「テンプレート改訂フロー」ステップ5 であり、そこでは `template/CLAUDE.md`「実装の実行方法
（Sonnetサブエージェント）」に準じた委任が明記されている（テンプレート側の update フローにある
「Claude Code 本体が行ってよい」という但し書きは、ルート `CLAUDE.md` には取り込まれていない）。

編集方式は **一意文字列の置換**とする。行番号は指定しない
（項目2 の1行追加により以降の行番号がずれるため、行番号ベースの編集は誤りを生みやすい）。
すべての置換前文字列について、置換直前の出現回数を §5 の手順で確認する。

### 2.1 サブエージェントへの指示に含めること

ルート `CLAUDE.md` ステップ5 の読み替えに従い、次を指示する。

1. **必読ドキュメントと読む順序**: ルート `CLAUDE.md` → 本案件の `README.md` → 本ファイル（`design.md`）
   → 変更対象のテンプレートファイル（`template/CLAUDE.md`）
2. **厳密準拠**: 本ファイル §3 に厳密に従うこと。書かれていない独自判断・改善・表現の調整は一切禁止
3. **想定外事象の扱い**: 置換前文字列の出現回数が §3 の記載と違う、置換が適用できない等の想定外事象が
   発生したら、**その場で回避策を実行せず直ちに中断**し、何が起きたか・どこまで完了したかを報告して終了する
4. **検証まで実施**: §5 の V1〜V5 を実行し、各期待値との一致を報告する（V6 のとおりテスト実行は不要）
5. **禁止事項**: git 操作（commit / push）を行わない。ルート `CLAUDE.md`・本案件の `README.md`・
   本ファイル・`reviews/` 配下を変更しない（完了処理は §6 のとおり Claude Code 本体が行う）
6. **報告形式**: 変更ファイル一覧、V1〜V5 の各実行結果と期待値との一致可否、想定外事象の有無

## 3. 変更の内容

### 3.1 項目1: レビュー単位の同一性を定める1文（1箇所・置換）

**対象**: 「Codexによるレビューの実行方法（Herdr 対話方式）」→「レビューの進め方」→「「収束」の定義」の
3段落目（反映前の 198行目）。

**置換前**（この部分文字列はファイル内に1回だけ出現することを 2026-09-05 に確認済み）:

```
criteria 文書ごとに1単位とする。新しいレビュー単位のレビューは
```

**置換後**:

```
criteria 文書ごとに1単位とする。同一の文書一式は、指摘を反映して内容を修正した後も同一のレビュー単位である（同じ一式を A に指定し直すこと・分割し直すこと・ストリームを変えることによって新しいレビュー単位にはならない）。新しいレビュー単位のレビューは
```

**根拠**: honOCR の `CLAUDE.md` 194行目（コミット `db95235` 時点）と同一の文面にする。
honOCR 側の設計は honOCR の `docs/issues/update-004-fix-review-rule-contradictions/design.md` §8.1。

### 3.2 項目2: 環境を変更する操作の事前承認ルール（1箇所・1行追加）

**対象**: 「開発方針」の箇条書きの末尾（反映前の 48行目の直後）。

**置換前**（この行はファイル内に1回だけ出現することを 2026-09-05 に確認済み）:

```
- 大きな機能を一度に作らない。小さく作って動作確認し、次の機能へ進む
```

**置換後**（2行。1行目は置換前と同一、2行目が追加分）:

```
- 大きな機能を一度に作らない。小さく作って動作確認し、次の機能へ進む
- **環境を変更する操作（パッケージのインストール、大容量データ・モデルのダウンロード、長時間ジョブの開始）は、必ず事前にユーザーの承認を得る**。調査・提案までは自律的に行ってよい
```

**honOCR の文面からの変更点（1箇所のみ）**: 例示「モデルのダウンロード」を
「**大容量データ・モデルのダウンロード**」に変更する。honOCR の文面は機械学習プロジェクトを
前提とした読みになるため、ルート `CLAUDE.md`「テンプレート保守の方針」の
「プロジェクト固有の記述（ドメイン知識・特定ライブラリ等）は `template/CLAUDE.md` に持ち込まず、
プレースホルダまたは汎用例に留める」に従い、機械学習以外のプロジェクトにも当てはまる例示にする。
他の語句（承認の対象・「調査・提案までは自律的に行ってよい」の但し書き）は honOCR と同一にする。

**この変更により、「開発方針」より後ろのすべての行番号が 1 大きくなる**（反映後のテンプレートは 348行）。
項目1 の対象箇所は「開発方針」より後ろにあるため、レビュー節の honOCR に対するオフセットは
**反映前の +4 から、反映後は +5 になる**。§5 の V4 はこれを前提とする。

### 3.3 項目3: `README.md` にローカルの実パスを書かない（2箇所・行末追記）

**対象**: 「機能追加フロー（feat-XXX 案件）」ステップ8（反映前の 64行目）と
「不具合修正フロー（bug-XXX 案件）」ステップ8（反映前の 77行目）。**両方に同一の追記を行う**
（feat フローと bug フローで規定を食い違わせないため）。

**置換前**（この文字列は行末としてファイル内に2回出現することを 2026-09-05 に確認済み。両方を置換する）:

```
に変更があった場合は `README.md` を最新に更新する
```

**置換後**:

```
に変更があった場合は `README.md` を最新に更新する。**リポジトリを公開する場合、`README.md` にはローカルの実パスを書かない**（`/home/...` 等の実パスは `{BASE}` などの抽象表現に置き換える）
```

**honOCR の文面からの変更点**: honOCR は理由を「公開リポジトリのため」と断定しているが、
テンプレートは公開・非公開のどちらのプロジェクトにも使われるため、**条件節「リポジトリを公開する場合、」を
文頭に置く形に変え、括弧内の理由の断定を削除する**。抽象表現の例（`{BASE}`）は honOCR と同一にする。

**注意**: この置換は2箇所とも同一文字列に対して行う。Edit ツールを使う場合は `replace_all: true` を
指定する（1箇所ずつ置換しようとすると一意性エラーになる）。

## 4. 反映の順序

行番号に依存しない文字列置換のため、**順序はどれからでもよい**。ただし §5 の検証手順は
3件すべての反映後に実行する。

## 5. 検証手順

反映後、以下をすべて実行し、記載の期待値と一致することを確認する。
`t` はテンプレート、`h` は honOCR の `CLAUDE.md` を指す。

```bash
t=/home/sakagawa/git/DEV_TEMPLATE/template/CLAUDE.md
h=/home/sakagawa/git/honOCR/CLAUDE.md
```

### V1: 行数

```bash
wc -l "$t"
```

期待値: **348**（反映前 347 ＋ 項目2 の1行）。

### V2: 3項目が反映されていること

```bash
grep -c '同一の文書一式は、指摘を反映して内容を修正した後も同一のレビュー単位である' "$t"   # 期待値: 1
grep -c '^- \*\*環境を変更する操作' "$t"                                                  # 期待値: 1
grep -c 'リポジトリを公開する場合' "$t"                                                    # 期待値: 2
```

### V3: 置換前文字列が残っていないこと

```bash
grep -c 'criteria 文書ごとに1単位とする。新しいレビュー単位のレビューは' "$t"              # 期待値: 0
grep -c 'に変更があった場合は `README.md` を最新に更新する$' "$t"                          # 期待値: 0（行末ではなくなるため）
```

### V4: honOCR とのレビュー節の一致（項目1 の検証）

```bash
diff <(sed -n '180,270p' "$h") <(sed -n '185,275p' "$t")
```

期待値: **無出力**（差分なし）。

**範囲指定の注意**: 反映前の比較範囲は `184,274p` だったが（そのときの差分は項目1 の1行 `15c15` のみ）、
項目2 の1行追加によりレビュー節の行番号が 1 大きくなるため、**反映後の範囲は `185,275p` を使う**
（オフセットは +4 から +5 に変わる。§3.2 参照）。反映前の範囲のまま実行すると、
範囲の先頭・末尾が1行ずれて差分が出るので誤りである。

### V5: 意図した変更のみであること

```bash
git -C /home/sakagawa/git/DEV_TEMPLATE diff --stat template/CLAUDE.md
git -C /home/sakagawa/git/DEV_TEMPLATE diff template/CLAUDE.md
```

期待値: `template/CLAUDE.md` のみが変更され、差分は **4 箇所・追加4行/削除3行**
（項目1 の1行置換 = 追加1/削除1、項目2 の1行追加 = 追加1/削除0、項目3 の2行置換 = 追加2/削除2）。
他のファイルに差分がないこと。

### V6: テスト

コード変更がないため、**自動テスト・手動テストとも不要**。

## 6. 完了処理

1. ルート `CLAUDE.md` の「ディレクトリ構成」は**更新しない**。本案件でファイルの追加・削除は
   `docs/issues/update-010-from-honocr-review-unit-identity/` の新設のみであり、当該構成は
   `docs/issues/` 配下を `update-{number}-{slug}/` のパターン表記で記載しているため、変更は不要である
   （コミット `6dc93ad` でパターン表記に変更済み）
2. 本案件の `README.md` のステータスを `Open（2026-09-05 起票）` から `Closed（2026-09-05 起票・完了）` に更新する
3. git 操作（コミット）は Opus サブエージェントに委任する（ルート `CLAUDE.md`「Claude Code 運用ルール」に従う）。
   コミット対象は `template/CLAUDE.md` と `docs/issues/update-010-from-honocr-review-unit-identity/` 配下一式
   （`README.md` / `design.md` / `reviews/codex-NN.result.md`）
4. 案件フォルダは完了後も削除・移動しない
5. 本リポジトリには `docs/BACKLOG.md` / `docs/CHANGELOG.md` が存在しないため、それらの更新は行わない
   （ルート `CLAUDE.md` のテンプレート改訂フローにも BACKLOG・CHANGELOG の更新は規定されていない）

## 7. 本案件のレビュー（ステップ4）の実施条件

ルート `CLAUDE.md`「テンプレート改訂フロー」ステップ4 に従う。

- ストリーム名: `rev-devtmpl-update-010`
- レビュー対象: 本フォルダの `README.md` と `design.md`
- レビュー観点（3点を明示して依頼する）:
  1. ドキュメント間の矛盾・参照切れ（存在しないファイル・セクションへの参照、フロー番号のずれ）
  2. テンプレートとしての汎用性（プロジェクト固有記述の混入、プレースホルダの不足・不整合）
  3. 新規プロジェクトへコピーした直後にそのまま運用できるか
- 結果の保存先: `reviews/codex-NN.result.md`（`full.log` は生成しない）

# codex-01: update-010 初回レビュー（A）

- 日付: 2026-09-05
- 対象ファイル: `docs/issues/update-010-from-honocr-review-unit-identity/README.md`、`docs/issues/update-010-from-honocr-review-unit-identity/design.md`
- ストリーム名: `rev-devtmpl-update-010`
- 依頼種別: **A: 初回レビュー**
- 直前に `/new` を送ったか: **No**（新規起動のため不要）
- ゲート状態: **未実施**
- 指摘数: 高 **0** / 中 **1** / 低 **0**
- 収束判定: **未収束（次: 指摘を全件反映 → B。同じ会話。`/new` はしない）**
- モデル: gpt-6-astra medium（`~/.codex/config.toml` の既定）
- トークン実測: `total_token_usage.total_tokens` = **108,683**（input 107,372 / cached_input 78,720 / output 1,311）
- rollout jsonl: `~/.codex/sessions/2026/09/05/rollout-2026-09-05T21-02-10-01a07172-99a9-78b0-9fae-f45b0cdea675.jsonl`

## 依頼文

```
以下のドキュメントをレビューせよ: docs/issues/update-010-from-honocr-review-unit-identity/README.md および docs/issues/update-010-from-honocr-review-unit-identity/design.md 。レビュー観点は次の3点: (1) ドキュメント間の矛盾・参照切れ（存在しないファイル・セクションへの参照、フロー番号のずれ）(2) テンプレートとしての汎用性（プロジェクト固有記述の混入、プレースホルダの不足・不整合）(3) 新規プロジェクトへコピーした直後にそのまま運用できるか。
```

## 回答（全文）

> [AGENTS.md適用]
>
> 指摘は 中1件（高0件・低0件） です。
>
> - **【中】反映後の行番号がずれ、正しく反映しても必須検証 V4 が失敗する**
>   対象: `docs/issues/update-010-from-honocr-review-unit-identity/design.md:143`（関連: 同文書 §3.2）
>
>   項目2で1行追加されるため、honOCR に対するレビュー節のオフセットは +4 から +5 に変わります。「反映後も+4」という説明は誤りです。指定の置換をメモリ上で再現したところ、現行の V4 は範囲の先頭・末尾に差分が生じ、「無出力」の必須条件を満たしませんでした。
>
>   修正案: テンプレート側の比較範囲を `185,275p` に変更し、§3.2・V4 の説明も「反映後は+5」に統一してください。この範囲では一致することを確認済みです。
>
>   ```
>   diff <(sed -n '180,270p' "$h") <(sed -n '185,275p' "$t")
>   ```
>
> 今回の変更がテンプレートの汎用性・新規プロジェクトでの運用を損なう重大な問題は、ほかに確認できませんでした。ファイルは変更していません。

## 指摘への対応（Claude Code 側）

指摘は妥当。項目2 の1行追加は「開発方針」（反映前の 48行目）で行われ、項目1 の対象箇所
（反映前の 198行目）より**前**にあるため、レビュー節のオフセットは +4 から +5 に変わる。
設計時の誤りであり、全件反映した。

1. `design.md` §3.2 の括弧書きを修正（「以降の行番号は honOCR と一致する」という誤った記述を、
   「開発方針より後ろのすべての行番号が 1 大きくなる／オフセットは +4 から +5 になる」に置換）
2. `design.md` §5 V4 の比較範囲を `184,274p` → `185,275p` に変更し、範囲指定の注意を追記
3. `README.md`「レビュー節の範囲比較」にも、反映後は範囲を `185,275p` に変える必要があることを追記
   （README の記載は反映前の実測値のため `184,274p` のまま残す）

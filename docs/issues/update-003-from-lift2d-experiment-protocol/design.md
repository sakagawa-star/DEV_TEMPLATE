# update-003 反映設計書: 「予測→実行→照合」プロトコルのテンプレート反映

## 1. 方針

- 変更内容の根拠（反映元の調査・差分の選別・汎用化調整の理由）は本案件 `README.md` を参照
- 実施者: **Sonnet サブエージェント**（本リポジトリ CLAUDE.md「テンプレート改訂フロー」ステップ5に従う）
- テスト: **コード変更がないため自動テスト・手動テストとも不要**
- 変更対象ファイル: `template/CLAUDE.md` の1ファイルのみ
- 変更しないファイル: `template/docs/` 配下の全ファイル、`template/.gitignore`、本リポジトリの `CLAUDE.md`（完了処理での要否判断は本書セクション4）
- 変更方式: 文字列挿入1件（P1）+ 文字列置換1件（P2）。全置換なし

## 2. template/CLAUDE.md の変更

### P1: 「実験・検証の進め方（予測→実行→照合）」節の新設（挿入）

`template/CLAUDE.md` 内の以下のアンカー行（「### ドキュメント更新フロー（update-XXX 案件）」配下「#### 運用メモ」の最終項目）:

```
- ルートの `.gitignore` がグローバル gitignore（`~/.gitignore_global`）の影響で未追跡になる環境では、コミットに含める際に `git add -f` が必要
```

と、その次の見出し行:

```
### ドキュメント作成ルール
```

の間に、以下のテキストを空行で挟んで挿入する（挿入後の並びは「運用メモ最終項目 → 空行 → 挿入テキスト → 空行 → ### ドキュメント作成ルール」となる）。

挿入テキスト（ここから）:

```
### 実験・検証の進め方（予測→実行→照合）

数値判定を伴う実験・検証（案件の調査フェーズでの実験、閾値・パラメータの妥当性検証、性能測定等）では、以下のプロトコルを**厳守**する。

1. **判定基準の事前定義**: 実験の実行前に「答える問い」「判定閾値」「合格ライン（何%・何件なら合格か）」を数値で criteria 文書（案件フォルダ配下、例: `docs/issues/{案件フォルダ}/experiments/{実験名}/criteria.md`）に文書化する。数値で定義できない場合は、その実験を「基準を測ることがゴールの実験」と明示的に再定義する（未知の量を既知のように扱わない）。criteria 文書は実行前に「Codexによるレビューの実行方法」に従って Codex レビューを行い（レビュー依頼時に対象の criteria 文書と関連ドキュメントを明示する。結果は案件フォルダの `reviews/` に案件の連番を進めて保存）、重要度「高・中」ゼロに収束させてから実験に着手する（criteria lock）
2. **直前予測**: 各フェーズの実行直前に、入出力の予測を数値で実験ログ（例: `experiment_log.md`）に記録してから実行する。予測は前段の実測結果を使って直前に立てる（全フェーズ一括の事前予測はしない——前段の結果で後段の内容が変わるため）。予測する項目の枠（何を予測するか）は criteria 文書に事前定義してよい（値はフェーズ直前に確定する）
3. **照合**: 実行後に予測と実測を照合して実験ログに記録する。乖離した場合はそのフェーズの前提・理解を疑い、原因を特定してから次フェーズに進む
4. **事後解釈の禁止**: 基準のない実験は「測定」であって「判定」ではない。結果を見てから合格ラインを定めることや、事前基準にない条件を付けて合格扱いにすること（事後解釈での Go/No-Go 判定）を行わない

対象外: 通常の実装・自動テスト（自動テストはテストコード自体が期待値＝予測の文書化に当たる）、および数値判定を伴わない調査（コード読解、ドキュメント調査等）。
```

（ここまで。コードフェンス自体は挿入しない）

反映元（lift2d `CLAUDE.md` の同名節）からの変更点は次の3点のみ（根拠は `README.md` の「差分1の汎用化調整」）:

1. 導入文の末尾「（feat-026 で試験導入し確立した運用。2026-07-31〜08-02）」を削除し、「〜を**厳守**する。」で文を終える
2. 対象外の説明「pytest はテストコード自体が」を「自動テストはテストコード自体が」に変更
3. 末尾の文「実運用の例は `docs/issues/feat-026-render-match-extrinsic-refinement/experiments/` の criteria 文書・experiment_log.md を参照。」を削除

### P2: 再レビュー節の `--last` の扱いの厳格化（置換）

`template/CLAUDE.md` の「#### 再レビュー（共通）」節にある以下の文字列:

```
それを明示指定するのが確実（`--last` でも可だが、別の codex 実行が挟まると意図しないセッションを掴む恐れがある）。連番を1つ進める:
```

を、以下に置換する（lift2d `CLAUDE.md` の同節と同一の文言）:

```
それを明示指定する（`--last` は別の codex 実行が挟まると意図しないセッションを掴む恐れがあるため使わない）。連番を1つ進める:
```

### 変更しない箇所（明示）

- `template/CLAUDE.md` の上記以外の全セクション（「案件ディレクトリ構成」の図に `experiments/` は追加しない。反映元 lift2d も追加しておらず、P1 本文中の例示パスで足りるため）
- `template/docs/` 配下の基準書・雛形の全ファイル（本プロトコルは CLAUDE.md のみで完結し、基準書側に対応する変更は反映元にも存在しない）

## 3. 実施手順（Sonnet サブエージェント向け）

1. 必読ドキュメントを順に読む: 本リポジトリ `CLAUDE.md` → 本案件 `README.md` → 本書（`design.md`） → `template/CLAUDE.md`
2. セクション2の P1（挿入）・P2（置換）を `template/CLAUDE.md` に適用する
3. 適用後、セクション5の検証を実施して結果を報告する
4. git 操作・本リポジトリ `CLAUDE.md` の更新・本書および案件 README の更新は行わない（完了処理は Claude Code 本体が行う）
5. アンカー文字列が見つからない等の想定外事象が発生したら、回避策を取らず直ちに中断して報告する

## 4. 完了処理（反映・検証後に Claude Code 本体が実施）

1. `git diff template/CLAUDE.md` で変更が P1 の挿入と P2 の置換のみであることを確認する
2. 本案件 `README.md` のステータスを `Open（2026-08-03 起票）` → `Closed（2026-08-03 起票、YYYY-MM-DD 完了）` に更新する
3. 本リポジトリ `CLAUDE.md` のディレクトリ構成: 更新不要（`docs/issues/` 配下は update-001 を「例」として示す形式であり全案件を列挙していない。`template/` 配下のファイル増減もない）
4. git コミット: 本リポジトリ `CLAUDE.md` の「Claude Code 運用ルール」に従い Opus サブエージェントに委任する

## 5. 反映後の検証

- `git diff template/CLAUDE.md` の変更箇所が2箇所（P1 の挿入・P2 の置換）のみで、既存セクションの削除・改変がないこと
- P1 の挿入位置が「#### 運用メモ」の最終項目と「### ドキュメント作成ルール」見出しの間であること
- 挿入テキストに反映元のプロジェクト固有記述（`feat-026`、`pytest`、日付）が含まれていないこと（`grep -n 'feat-026\|pytest' template/CLAUDE.md` がヒット0件であること。ただし `{{例: uv run pytest -v}}` などテンプレート既存のプレースホルダ例は除く——検証時は P1 挿入範囲内にヒットがないことを確認する）
- P2 の置換後の文言が lift2d `CLAUDE.md` の「#### 再レビュー（共通）」節と一致すること

## 改訂履歴

- 2026-08-03: 初版（Codex レビュー前）
- 2026-08-03: codex-01 指摘対応（高: 案件番号が既存の `update-002-from-synchrocap` と衝突していたため、案件フォルダを `update-003-from-lift2d-experiment-protocol` にリネームし、README / design のタイトルを update-003 に変更。README に本リポジトリと反映元の案件番号の対応を明記）

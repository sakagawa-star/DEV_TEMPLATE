# AGENTS.md

このファイルは Codex CLI が起動時に自動で読み込む指示ファイルである
（対象: このリポジトリを作業ディレクトリとして起動されたすべての codex セッション）。

## レビューを依頼された場合

- 瑣末な点への指摘はせず、致命的な点のみ指摘する
- 発見した問題を重要度(高/中/低)で分類し、修正提案とともに報告する
- レビュー回答の冒頭に必ず「[AGENTS.md適用]」と表示する（依頼側が本ファイルの
  読み込みを確認するためのマーカー）
- レビュー対象に `docs/issues/` 配下の案件文書が含まれる場合、対象文書が属する各案件フォルダ
  `docs/issues/update-{number}-{slug}/` について、CLAUDE.md「テンプレート改訂フロー（update-XXX 案件）」と、
  その節から参照される節（ステップ4 の読み替えで適用する `template/CLAUDE.md`「Codexによるレビューの
  実行方法（Herdr 対話方式）」の各小節）を読む（対象が複数の案件フォルダにまたがる場合は、該当する
  全案件フォルダについて行う）。判定は次の2段階で行い、不適合は他の指摘と同じ重要度分類で報告する。
  判定に必要な情報（工程、文書の種類と規定の対応）は下記の情報源から取り、そこから一意に決まらない
  場合は推測せず「判定不能」とその理由を**高**で報告する（判定不能が残る間はレビューを収束扱いにしない）:
  1. 案件フォルダ単位: テンプレート改訂フローのステップ4（レビュー）の時点で存在すべき文書
     （`README.md`・`design.md`）が揃い、規定の場所に置かれているか（存在してはならない文書がないかを
     含む。`reviews/` は結果の保存先であり、存在の判定対象にしない）。工程は常にステップ4 とする
  2. 対象文書単位: 依頼で指定された各対象文書に、規定された内容があり、書いてはならない内容が
     含まれていないか。`README.md` はステップ2 が定める内容、`design.md` はステップ3 が定める内容に従う
     （判定対象は依頼で指定された文書のみ。`reviews/` 配下の結果文書と、依頼で指定されていない文書は対象外）
